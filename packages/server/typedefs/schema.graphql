type AuthPayload {
  token: String
}

type UserInfoPayload {
  name: String
  roles: [String]
}

type Mutation {
  login(id_token: String!): AuthPayload
}

type Query {
  currentUser: UserInfoPayload!
}