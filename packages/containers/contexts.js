import { createContext } from 'react'

export const UserContext = createContext('User')
export const ApolloContext = createContext('Apollo')
export const MembershipsContext = createContext('Memberships')