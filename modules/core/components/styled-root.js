import styled from 'styled-components'

const StyledRoot = styled.div`
  && {
    flex-grow: 1;
    z-index: 1;
    overflow: hidden;
    position: relative;
    display: flex;
    width: 100%;
    height: 100%;
  }
`

export default StyledRoot;
